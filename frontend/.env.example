# Base URL for the backend API
# During development, Vite's proxy handles this — set to /api
# After deploying to Render/Railway, replace with your actual backend URL
VITE_API_URL=http://localhost:5000/api
